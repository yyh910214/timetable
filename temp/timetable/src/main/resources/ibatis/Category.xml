<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap>
    <insert id="insertCategories" parameterClass="java.util.List">	
    	INSERT
    	INTO hufs_category(catg_id, catg_name, campus_code, gubun)
    	VALUES
		<iterate conjunction=",">
			(#[].categoryId#, #[].categoryName#, #[].categoryCampus#, #[].categoryGubun#)
		</iterate>
    </insert>
    
    <select id="getCatgCode" parameterClass="java.util.Map" resultClass="java.lang.String">	
    	SELECT catg_id
		FROM hufs_category
        WHERE gubun = #majorCode# AND campus_code = #campusCode#
    </select>
    
    <select id="getAllCategoryByGroup" parameterClass="Category" resultClass="Category">	
    	SELECT catg_id as categoryId,
    	catg_name as categoryName,
    	campus_code as categoryCampus,
    	gubun as categoryGubun
		FROM hufs_category
		WHERE campus_code=#categoryCampus# AND gubun=#categoryGubun#
    </select>
    
    
    <select id="getAllCategory" resultClass="Category">	
    	SELECT catg_id as categoryId,
    	catg_name as categoryName,
    	campus_code as categoryCampus,
    	gubun as categoryGubun
		FROM hufs_category
    </select>
    
    <select id="getCategoryCount" resultClass="Integer">
    	SELECT COUNT(*)
    	FROM hufs_category
    </select>
    
    <delete id="clearCategory">
		DELETE
		FROM hufs_category
	</delete>
	
	<select id="getMajorCategories" resultClass="Category">
		SELECT catg_id as categoryId,
    	catg_name as categoryName,
    	campus_code as categoryCampus,
    	gubun as categoryGubun
    	FROM hufs_category
    	WHERE gubun=1
	</select>

</sqlMap>